# Django core
Django==5.0.6
djangorestframework==3.15.2
django-environ==0.11.2
django-cors-headers==4.4.0

# Auth
django-allauth>=0.63,<0.64
dj-rest-auth>=7.0.0,<7.1
djangorestframework-simplejwt==5.3.1   # ← без [blacklist]; блэклист уже в INSTALLED_APPS

# HTTP / OAuth deps
requests>=2.31,<3
cryptography>=42,<43

# DB
psycopg[binary]==3.1.19               # ← вернули бинарные колёса, без pg_config
# если очень хочешь C-ускоритель, то:
# psycopg[c]==3.1.19
# и в Dockerfile нужно добавить: apt-get install -y libpq-dev

# Tasking / infra
redis==5.0.7
celery==5.3.6

# ASGI / WSGI servers
gunicorn==22.0.0
uvicorn==0.30.3
Pillow==10.4.0
